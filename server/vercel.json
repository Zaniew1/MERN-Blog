{
    "buildCommand":"npm i && npm run build",
    "outputDirectory":"server/dist",
    "rewrites": [
        {
            "source": "/server/(.*)",
            "destination": "/server/dist/index.js"
        }
    ]
}